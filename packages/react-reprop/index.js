throw new Error([
    "Reprop's React integration `react-reprop` is work in progress.",
    "See https://github.com/brillout/reprop/issues/1",
    "You can use `reprop`, Reprop's standalone version, in the meantime.",
].join('\n'));
